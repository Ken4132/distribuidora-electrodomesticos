# URL del backend cuando el frontend NO usa el proxy de Vite
# (por ejemplo en producción, sirviendo el build detrás de otro dominio).
# En desarrollo se puede dejar vacío: vite.config.js hace proxy de /api.
VITE_API_URL=

# Destino del proxy en desarrollo
VITE_API_TARGET=http://localhost:4000
